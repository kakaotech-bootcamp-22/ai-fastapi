<실행 방법>
1. models 폴더에 최종 체크포인트.tar 파일 업로드
2. uvicorn main:app --reload로 로컬 서버 실행

<파일 배치 및 역할 정리>
- models/ : 모델 관련 파일
- utils/preprocess.py : 모델 로드 및 데이터 전처리 코드 관리
- main.py : API 엔드포인트를 정의 및 서버를 실행 담당


학습이 완료된 모델을 .pt 파일로 저장
로컬 서버로 이동한 뒤 FastAPI 서버에서 직접 로드하여 사용하는 것을 권장

<환경 설정 ?>
python -m venv venv
source venv/bin/activate  # Mac/Linux
pip install -r requirements.txt


<모델 로드 방식 장단점>
1. 체크포인트(.tar)를 사용하는 경우
- 모델을 아직 학습 중이고 추가 학습 가능성이 있으면 (V)
- 학습 중단 시점에서 복원하여 학습을 재개하려는 경우
- 학습 과정을 추적하거나 여러 상태를 저장해야 하는 경우

2. .pt 파일을 사용하는 경우
- 모델 학습이 완료된 후 배포 또는 에측에만 사용할 경우 (추후 이 방식으로 변경 ?)
- 단순히 가중치를 로드하려는 경우
- 파일 크기와 로드 속도가 중요할 때

실무에서의 일반적 패턴 : 학습 과정에서는 .tar 파일 사용하다가 학습 완료되면 .pt 파일로 최종 모델 저장


<메모>
- 키위 라이브러리 사용시 파이썬 버전 불일치로 인한 설치 불가 문제 -> 대체 라이브러리 사용


